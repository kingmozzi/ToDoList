<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="360" Width="360">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Name="btnAdd" Grid.Column="0" Width="40" HorizontalAlignment="Left" Background="Transparent" Content="+" 
                    FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="btnAdd_Click"/>
            <Button Name="btnCalendar" Grid.Column="1" Width="40" HorizontalAlignment="Left" Background="Transparent" Content="Cal" 
                    FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="btnCalendar_Click"/>
            <Button Name="btnExit" Grid.Column="8" Width="40" HorizontalAlignment="Right" Background="Transparent" Content="x" 
                    FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="btnExit_Click"/>

        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Today" FontSize="20"/>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem ="{Binding SelectedItem}" CanUserAddRows="False"
                      CanUserDeleteRows="False" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="4*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="2*"/>
                    <DataGridTextColumn Header="Priority" Binding="{Binding Prioritye}" Width="2*"/>
                    <DataGridTextColumn Header="is done?" Binding="{Binding is_done}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>

    </Grid>
</Window>

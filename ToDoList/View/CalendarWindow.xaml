<Window x:Class="ToDoList.View.CalendarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList.View"
        mc:Ignorable="d"
        Title="CalendarWindow" Height="360" Width="360">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="btnExit" Width="40" Height="40" HorizontalAlignment="Right" Content="x" FontSize="20" 
                Background="Transparent" Click="btnExit_Click"/>

        <Calendar SelectedDate="{Binding SelectedDate}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Calendar.LayoutTransform>
                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
            </Calendar.LayoutTransform>
        </Calendar>

    </Grid>
</Window>
